require('dotenv').config({ path: 'appWeb/appBackend/.env' }); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_KEY); async function check() { const deckId = '899dc8c5-2d46-4239-9b84-98cd7ee80a06'; const { data: deck } = await supabase.from('shared_decks').select('*').eq('deck_id', deckId); console.log('Deck:', JSON.stringify(deck, null, 2)); const { data: invites } = await supabase.from('deck_invites').select('*').eq('deck_id', deckId); console.log('Invites:', JSON.stringify(invites, null, 2)); } check();
